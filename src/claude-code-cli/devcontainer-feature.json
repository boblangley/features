{
  "id": "claude-code-cli",
  "version": "1.0.0",
  "name": "Claude Code CLI",
  "documentationURL": "https://github.com/boblangley/features/tree/main/src/claude-code-cli",
  "description": "Installs Claude Code using Anthropic's native Linux binary installer path and ensures `claude` is on the PATH.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Claude Code version to install. Use `latest` or a specific version like `1.0.58`."
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
