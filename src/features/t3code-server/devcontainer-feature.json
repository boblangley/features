{
  "id": "t3code-server",
  "version": "2.2.0",
  "name": "T3 Code Server",
  "documentationURL": "https://github.com/wyrd-company/devcontainers/tree/main/src/features/t3code-server",
  "description": "Installs T3 Code globally and configures an s6-overlay service that runs as the selected user on Debian/Ubuntu images.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "T3 Code npm package version to install."
    },
    "port": {
      "type": "string",
      "default": "3773",
      "description": "Port exposed by the T3 Code server."
    },
    "host": {
      "type": "string",
      "default": "0.0.0.0",
      "description": "Interface to bind the T3 Code server to."
    },
    "serveMode": {
      "type": "string",
      "enum": ["", "web", "desktop"],
      "default": "",
      "description": "Optional T3 runtime mode passed to `t3 serve --mode`. Set to `web` for the web runtime. Empty preserves the T3 CLI default."
    },
    "serviceUser": {
      "type": "string",
      "default": "automatic",
      "description": "User account to run the service as. `automatic` prefers the remote user, container user, `vscode`, then `root`."
    },
    "dnsName": {
      "type": "string",
      "default": "",
      "description": "Optional fully qualified DNS name exposed through the Caddy Feature, such as `t3.dev-environment.example.test`."
    }
  },
  "dependsOn": {
    "ghcr.io/devcontainers/features/node:2": {
      "version": "24"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils",
    "ghcr.io/wyrd-company/devcontainers/caddy"
  ]
}
