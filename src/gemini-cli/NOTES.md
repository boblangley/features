Installs the npm-distributed Gemini CLI and makes `gemini` available on the container `PATH`.

Example usage:

```json
{
  "features": {
    "ghcr.io/wyrd-company/features/gemini-cli:1": {}
  }
}
```
